pipeline {
  agent {
    kubernetes {
      yamlFile 'jenkins/jobs/KubernetesPod.yaml'
    }
  }
  stages {
    stage('Run python') {
      steps {
        container('python') {
          sh 'python --version'
        }
      }
    }
  }
}

