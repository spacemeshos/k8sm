pipeline {
  agent {
    kubernetes {
      yamlFile 'KubernetesPod.yaml'
    }
  }
  stages {
    stage('Run python') {
      steps {
        container('python') {
          sh 'python --version'
        }
      }
    }
  }
}

